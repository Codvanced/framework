printer = () -> { identifier; repeat; }
printer.prototype.getIdentifier = () =>  this.identifier
printer.prototype.setIdentifier = (_identifier) =>  this.identifier = _identifier
printer.prototype.getRepeat = () =>  this.repeat
printer.prototype.setRepeat = (_repeat) =>  this.repeat = _repeat
printer.prototype.print = () => { 
	loops; 
	id; 
	repeat: this.repeat;  
	
	console log('a') 
		while loops++ < repeat  
}

